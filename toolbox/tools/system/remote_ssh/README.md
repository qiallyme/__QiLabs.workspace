# Remote Ssh

## Purpose

`remote_ssh.py` is a QiOne Desktop Tools module in the `system` bucket.

## Toolbox Metadata

- **Tool ID:** `toolbox.system.remote_ssh`
- **Bucket:** `system`
- **Entrypoint class:** `UNKNOWN`
- **Source file:** `remote_ssh.py`

## Usage

This tool is loaded by the QiOne Desktop Tools launcher.

Typical import:

```python
from tools.system.remote_ssh import ToolClass
```

## Safety

Use scan/dry-run mode before live execution when the tool supports it.

## Files

```txt
remote_ssh/
  __init__.py
  remote_ssh.py
  README.md
  manifest.yaml
```
