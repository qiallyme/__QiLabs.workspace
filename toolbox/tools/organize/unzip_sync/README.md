# Unzip Sync

## Purpose

`unzip_sync.py` is a QiOne Desktop Tools module in the `organize` bucket.

## Toolbox Metadata

- **Tool ID:** `toolbox.organize.unzip_sync`
- **Bucket:** `organize`
- **Entrypoint class:** `UnzipSyncTool`
- **Source file:** `unzip_sync.py`

## Usage

This tool is loaded by the QiOne Desktop Tools launcher.

Typical import:

```python
from tools.organize.unzip_sync import UnzipSyncTool
```

## Safety

Use scan/dry-run mode before live execution when the tool supports it.

## Files

```txt
unzip_sync/
  __init__.py
  unzip_sync.py
  README.md
  manifest.yaml
```
